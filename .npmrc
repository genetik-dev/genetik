# Use a flat node_modules layout so VS Code extensions (e.g. Prettier) can resolve
# packages. pnpm's default "isolated" layout uses symlinks from .pnpm/ which
# some tools resolve incorrectly.
node-linker=hoisted
